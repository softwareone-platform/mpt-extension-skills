interface:
  display_name: "Jira Workitem Ops"
  short_description: "Safe Jira workitem ops via acli CLI"
  default_prompt: "Handle Jira issue operations end-to-end: read/create/edit/comment/assign/search/link work items with safe defaults. Verify auth and project context, ask for missing issue keys and mandatory attributes before writes, read before write, enforce assignee safety by default, use bundled scripts for deterministic ADF payloads, include \"🤖 Generated by AI\" attribution in generated descriptions/comments, and report exact permission or validation blockers."
