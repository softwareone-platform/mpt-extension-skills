interface:
  display_name: "Dependabot PR Policy Fix"
  short_description: "Fix Dependabot dependency policy issues in upstream extension PRs"
  default_prompt: "Review open Dependabot pull requests in a softwareone-platform extension repository, apply the shared dependency-management and validation workflow, sync pre-commit dependency pins when dev dependencies change, revert opentelemetry-family dependency bumps, amend the existing Dependabot branch, and report the outcome."
