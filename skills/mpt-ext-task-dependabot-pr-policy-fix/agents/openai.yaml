interface:
  display_name: "Dependabot PR Policy Fix"
  short_description: "Apply dependency-policy fixes to one selected Dependabot PR or checked-out branch"
  default_prompt: "Apply dependency-policy fixes to a selected Dependabot PR or checked-out branch: sync pre-commit dependency pins when relevant dev dependencies change, revert opentelemetry-family bumps, refresh lock state through the repository workflow, and report changed files and fixed rules without validating, amending, or pushing."
