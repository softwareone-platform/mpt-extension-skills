interface:
  display_name: "Open Pull Request"
  short_description: "Open or update only the pull request"
  default_prompt: "Open or update a repository-compliant pull request: inspect branch PR state, read repository PR rules, build the PR title and description from those rules, create or update the PR without duplication, and use the bundled script for compact result output."
