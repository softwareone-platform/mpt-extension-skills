interface:
  display_name: "Start Work"
  short_description: "Run the whole start-of-work flow (branch plus Jira)"
  default_prompt: "Start work on a Jira issue by creating the correctly named work branch from Jira context and then moving the Jira issue into active development state through the shared branch-creation and Jira-start tasks."
