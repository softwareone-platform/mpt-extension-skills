interface:
  display_name: "Skill Authoring"
  short_description: "Create or update reusable shared skills"
  default_prompt: "Create or update a reusable shared skill. Classify it as tool, task, or workflow; apply the shared naming and structure rules; keep the skill concise and reusable; add or update the agents/openai.yaml adapter to keep it in sync; and separate skill-specific behavior from shared standards or knowledge."
