interface:
  display_name: "Start Jira Work"
  short_description: "Move only Jira state into active development"
  default_prompt: "Start Jira work on an issue: move the issue and its full parent chain to In Progress, resolve and apply the active sprint placement (verifying inherited placement for subtasks), and ask before reassigning the issue to the current Jira-authenticated user."
