interface:
  display_name: "Git Branch Ops"
  short_description: "Low-level create or switch for a known branch name"
  default_prompt: "Handle Git branch operations for starting work: use the bundled script to resolve the base branch from the branch type, inspect the worktree, stop for dirty state or branch conflicts, update the base branch locally, and create the target branch safely."
