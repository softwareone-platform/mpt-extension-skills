interface:
  display_name: "Create Work Branch"
  short_description: "Create a Jira-based work branch"
  default_prompt: "Create a work branch from Jira issue context: read the issue, use the bundled script to render the short branch description and target branch name, then create the branch through the shared Git branch tool skill."
