---
description: Carry a main PR change to the active release branch as a hotfix or backport — cherry-pick, validate, open the release PR, and keep Jira accurate.
argument-hint: <JIRA-KEY>
arguments: [issue]
---

Use the `mpt-ext-workflow-hotfix-backport` skill to prepare the hotfix/backport for $issue against the active release branch.

If the key is missing, ask for it before proceeding. Follow the skill exactly.
